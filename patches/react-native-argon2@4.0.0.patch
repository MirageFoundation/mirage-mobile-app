diff --git a/android/src/main/java/com/poowf/argon2/RNArgon2Module.java b/android/src/main/java/com/poowf/argon2/RNArgon2Module.java
index 5e683d570479d52a8b64bbf1f0f30ee32dfe8cef..603d31b2762a69fa14e3d1af8306faae13043d2f 100644
--- a/android/src/main/java/com/poowf/argon2/RNArgon2Module.java
+++ b/android/src/main/java/com/poowf/argon2/RNArgon2Module.java
@@ -1,5 +1,6 @@
 package com.poowf.argon2;
 
+import android.util.Base64;
 import android.app.Activity;
 import android.content.Intent;
 
@@ -63,7 +64,17 @@ public class RNArgon2Module extends ReactContextBaseJavaModule {
     @ReactMethod
     public void argon2(String password, String salt, ReadableMap config, Promise promise) {
         try {
-            final byte[] passwordBytes = password.getBytes("UTF-8");
+            String passwordEncoding = config.hasKey("passwordEncoding") ? config.getString("passwordEncoding") : "utf8";
+            final byte[] passwordBytes;
+            if ("hex".equalsIgnoreCase(passwordEncoding)) {
+                passwordBytes = hexStringToByteArray(password);
+            } else if ("base64".equalsIgnoreCase(passwordEncoding) || "b64".equalsIgnoreCase(passwordEncoding)) {
+                passwordBytes = Base64.decode(password, Base64.DEFAULT);
+            } else if ("latin1".equalsIgnoreCase(passwordEncoding) || "iso-8859-1".equalsIgnoreCase(passwordEncoding) || "binary".equalsIgnoreCase(passwordEncoding)) {
+                passwordBytes = password.getBytes("ISO-8859-1");
+            } else {
+                passwordBytes = password.getBytes("UTF-8");
+            }
             
             String saltEncoding = config.hasKey("saltEncoding") ? config.getString("saltEncoding") : "utf8";
             final byte[] saltBytes = ("hex".equalsIgnoreCase(saltEncoding)) ? hexStringToByteArray(salt) : salt.getBytes("UTF-8");
diff --git a/index.d.ts b/index.d.ts
index b8050efbe795ff7e5643e0faa1c02e6c7d61319e..a30f4aa2c723d9cecd021d59ea7d5fa5bc8a7463 100644
--- a/index.d.ts
+++ b/index.d.ts
@@ -7,6 +7,9 @@ export interface Argon2Options {
   mode?: Argon2Mode;
 // 'utf8' (default, for backward compatibility)
   saltEncoding?: 'utf8' | 'hex';
+// 'utf8' (default). Use 'latin1' to interpret each codepoint 0-255 as a byte (ISO-8859-1),
+// or 'hex'/'base64' to decode the password string into bytes before hashing.
+  passwordEncoding?: 'utf8' | 'latin1' | 'hex' | 'base64';
 }
 
 export interface Argon2Result {
diff --git a/ios/RNArgon2.swift b/ios/RNArgon2.swift
index a9aad3fa58762ddcd7cddc416544918b53c1a88f..6a45adb6a9a393d52cce4483b708db1ef65f6fcc 100644
--- a/ios/RNArgon2.swift
+++ b/ios/RNArgon2.swift
@@ -33,9 +33,26 @@ class RNArgon2: NSObject {
     let saltObject = Salt(bytes: validSaltData)
     
     do {
+      let passwordEncoding = (configDict["passwordEncoding"] as? String ?? "utf8").lowercased()
+      let passwordData: Data?
+      if passwordEncoding == "hex" {
+        passwordData = Data(hexString: password)
+      } else if passwordEncoding == "base64" || passwordEncoding == "b64" {
+        passwordData = Data(base64Encoded: password)
+      } else if passwordEncoding == "latin1" || passwordEncoding == "iso-8859-1" || passwordEncoding == "binary" {
+        passwordData = password.data(using: .isoLatin1)
+      } else {
+        passwordData = password.data(using: .utf8)
+      }
+      guard let validPasswordData = passwordData else {
+        let error = NSError(domain: "com.poowf.argon2", code: 400, userInfo: ["Error reason": "Invalid password format"])
+        reject("E_ARGON2", "Invalid password format", error)
+        return
+      }
+
       // Generate hash using Argon2Swift
-      let result = try Argon2Swift.hashPasswordString(
-        password: password,
+      let result = try Argon2Swift.hashPasswordBytes(
+        password: validPasswordData,
         salt: saltObject,
         iterations: iterations,
         memory: memory,
