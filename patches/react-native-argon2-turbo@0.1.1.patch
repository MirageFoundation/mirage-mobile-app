diff --git a/android/src/main/AndroidManifest.xml b/android/src/main/AndroidManifest.xml
index 7e8e5c3..c4e8f21 100644
--- a/android/src/main/AndroidManifest.xml
+++ b/android/src/main/AndroidManifest.xml
@@ -1,2 +1,3 @@
 <manifest xmlns:android="http://schemas.android.com/apk/res/android">
+    <uses-permission android:name="android.permission.WAKE_LOCK" />
 </manifest>
diff --git a/android/src/main/java/com/argon2turbo/Argon2TurboModule.kt b/android/src/main/java/com/argon2turbo/Argon2TurboModule.kt
index 7454db38da14cacfa3fc64f9a939e0afee760d56..a4accec5c4889e0e20a0126a138449213c4eea33 100644
--- a/android/src/main/java/com/argon2turbo/Argon2TurboModule.kt
+++ b/android/src/main/java/com/argon2turbo/Argon2TurboModule.kt
@@ -1,5 +1,7 @@
 package com.argon2turbo
 
+import android.content.Context
+import android.os.PowerManager
 import com.facebook.react.bridge.Arguments
 import com.facebook.react.bridge.Promise
 import com.facebook.react.bridge.ReactApplicationContext
@@ -19,6 +21,8 @@ import kotlinx.coroutines.selects.select
 import kotlinx.coroutines.cancelChildren
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicInteger
+import kotlin.math.max
+import kotlin.math.min
 
 data class PowWorkerResult(
     val nonce: UInt,
@@ -35,14 +39,15 @@ class Argon2TurboModule(reactContext: ReactApplicationContext) :
     private val totalAttempts = AtomicInteger(0)
     private var powStartTime: Long = 0
     private var powJob: Job? = null
+    private var wakeLock: PowerManager.WakeLock? = null
     private val scope = CoroutineScope(Dispatchers.Default)
     
     // Store worker attempts for real-time progress tracking
     @Volatile
     private var workerAttemptsArray: Array<AtomicInteger>? = null
     
-    // Number of parallel workers for PoW
-    private val NUM_WORKERS = 4
+    // Cap workers so mid-range / little-core-heavy SoCs are not oversubscribed.
+    private val NUM_WORKERS = min(4, max(2, Runtime.getRuntime().availableProcessors()))
 
     override fun getName(): String = NAME
 
@@ -181,6 +186,7 @@ class Argon2TurboModule(reactContext: ReactApplicationContext) :
         cancelFlag.set(false)
         totalAttempts.set(0)
         powStartTime = System.currentTimeMillis()
+        acquirePowWakeLock(timeoutMs.toLong())
 
         val requiredBits = difficulty.toInt()
         val maxAttemptsInt = maxAttempts.toInt()
@@ -252,8 +258,13 @@ class Argon2TurboModule(reactContext: ReactApplicationContext) :
                     totalAttempts.set(total)
                 }
 
-                // Cancel remaining workers
-                cancelFlag.set(true)
+                // Preserve whether cancellation came from the caller. Setting
+                // this unconditionally made normal max-attempt exhaustion look
+                // like POW_CANCELLED to JavaScript.
+                val wasCancelled = cancelFlag.get()
+                if (result != null) {
+                    cancelFlag.set(true)
+                }
                 coroutineContext.cancelChildren()
 
                 val elapsedMs = System.currentTimeMillis() - powStartTime
@@ -271,7 +282,7 @@ class Argon2TurboModule(reactContext: ReactApplicationContext) :
                     }
                 } else {
                     withContext(Dispatchers.Main) {
-                        if (cancelFlag.get()) {
+                        if (wasCancelled) {
                             promise.reject("POW_CANCELLED", "PoW computation was cancelled")
                         } else {
                             promise.reject("POW_MAX_ATTEMPTS", "Max attempts exceeded")
@@ -282,6 +293,8 @@ class Argon2TurboModule(reactContext: ReactApplicationContext) :
                 withContext(Dispatchers.Main) {
                     promise.reject("POW_ERROR", e.message)
                 }
+            } finally {
+                releasePowWakeLock()
             }
         }
     }
@@ -346,8 +359,36 @@ class Argon2TurboModule(reactContext: ReactApplicationContext) :
     override fun cancelPow() {
         cancelFlag.set(true)
         powJob?.cancel()
+        releasePowWakeLock()
     }
 
+    private fun acquirePowWakeLock(timeoutMs: Long) {
+        releasePowWakeLock()
+        try {
+            val powerManager = reactApplicationContext.getSystemService(Context.POWER_SERVICE) as? PowerManager
+                ?: return
+            val lock = powerManager.newWakeLock(PowerManager.PARTIAL_WAKE_LOCK, "Argon2Turbo:PoW")
+            lock.setReferenceCounted(false)
+            lock.acquire(max(5_000L, timeoutMs + 5_000L))
+            wakeLock = lock
+        } catch (_: Exception) {
+            wakeLock = null
+        }
+    }
+
+    private fun releasePowWakeLock() {
+        try {
+            wakeLock?.let { lock ->
+                if (lock.isHeld) {
+                    lock.release()
+                }
+            }
+        } catch (_: Exception) {
+        } finally {
+            wakeLock = null
+        }
+    }
+
     override fun getPowProgress(promise: Promise) {
         // Sum up attempts from all workers for real-time progress
         val attempts = workerAttemptsArray?.sumOf { it.get() } ?: totalAttempts.get()
diff --git a/ios/Argon2Turbo.mm b/ios/Argon2Turbo.mm
index 4ad288582cc611957af043e1728351ca370048e8..e4c8343a259acf96bd33567e411d9c94f89bf47c 100644
--- a/ios/Argon2Turbo.mm
+++ b/ios/Argon2Turbo.mm
@@ -244,6 +244,15 @@ - (void)computePow:(NSString *)base
     dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
         dispatch_time_t timeout = dispatch_time(DISPATCH_TIME_NOW, (int64_t)(timeoutMs * NSEC_PER_MSEC));
         long waitResult = dispatch_group_wait(group, timeout);
+
+        // Workers still reference foundResultPtr and workerAttemptsPtr. Stop
+        // and join them before reading or freeing either allocation; otherwise
+        // a timeout can race into a use-after-free, especially when a retry
+        // starts immediately afterward.
+        if (waitResult != 0) {
+            self->_cancelFlag.store(true);
+            dispatch_group_wait(group, DISPATCH_TIME_FOREVER);
+        }
         
         // Calculate total attempts
         int totalAttempts = 0;
