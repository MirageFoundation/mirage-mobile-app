diff --git a/ios/VideoPlayer.swift b/ios/VideoPlayer.swift
index 847500d01211d9ba670220046a0adadc837b056f..c77a9f16761db1ea0578c090ee58a25c301740ff 100644
--- a/ios/VideoPlayer.swift
+++ b/ios/VideoPlayer.swift
@@ -418,2 +418,7 @@ internal final class VideoPlayer: SharedRef<AVPlayer>, Hashable, VideoPlayerObse
-    if self.appContext != nil {
-      self.emit(event: event, arguments: payload?.toDictionary(appContext: appContext))
+    // Some AVPlayer callbacks arrive on background Swift executors. Serializing
+    // Expo record payloads there can race with player teardown and crash fieldsOf.
+    let emitOnMain = { [weak self] in
+      guard let self, self.appContext != nil else {
+        return
+      }
+      self.emit(event: event, arguments: payload?.toDictionary(appContext: self.appContext))
@@ -420,0 +426 @@ internal final class VideoPlayer: SharedRef<AVPlayer>, Hashable, VideoPlayerObse
+    Thread.isMainThread ? emitOnMain() : DispatchQueue.main.async(execute: emitOnMain)
diff --git a/ios/VideoPlayerObserver.swift b/ios/VideoPlayerObserver.swift
index da422f893203ab645d7d7470b66fda8f50d06070..82f094200c0ed5e0f37067dd580ef198f3a98c28 100644
--- a/ios/VideoPlayerObserver.swift
+++ b/ios/VideoPlayerObserver.swift
@@ -85,9 +85,3 @@ class VideoPlayerObserver: VideoSourceLoaderListener {
-  private var currentVideoTrack: VideoTrack? {
-    didSet {
-      if let player, oldValue != currentVideoTrack {
-        delegates.forEach { delegate in
-          delegate.value?.onVideoTrackChanged(player: player, oldVideoTrack: oldValue, newVideoTrack: currentVideoTrack)
-        }
-      }
-    }
-  }
+  // Expo SDK 54 can crash while serializing videoTrackChange events under
+  // rapid HLS track changes. Mirage does not consume this event.
+  private var currentVideoTrack: VideoTrack?
